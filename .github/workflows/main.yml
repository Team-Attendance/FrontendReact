name: Amateur
on:
  push:
    branches:
      - main
      
jobs:
  build:
    runs-on: ubuntu-lastest
    steps:
      - name: Checkout source code.
        uses: actions/checkout@main
        
      - name: Install Dependencies
        run: npm install
        
      - name: Build
        run: npm run build
